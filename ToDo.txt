# Reload
Should check KB of each installation - is service stack and cumulative alway the same KB? 
check instalation status

# Schedual updates
timestamp may be wrong
make the repeat work
save the users email
select multiple dates?

this print should happen only when okay is pressed
        if scheduled_datetime <= QDateTime.currentDateTime():
            QMessageBox.warning(self, "Invalid Time", "Please select a future time.")
            return  # <-- Don't close the window or save anything

# Update all
make it update all
should update instalation status while running

# Update selected
same as all but for selected machines

# Edit Help Center

# Add reboot option?

# Resize table with window

# disk space
ccheck iiif there is enough space for installation
if not ennough space - bar should be red
